<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="px-3 py-2 bg-red-500 text-sm rounded-md" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post.</h5>
        <form action="/update/<%= post._id%>" method="post">
            <textarea placeholder="what's on your mind ?" class="outline-none w-1/3 p-3 block resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id="" required><%= post.content%></textarea>
            <input class="px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40 text-sm" type="submit" value="Edit Post" >
        </form>
    </div>
</body>
</html>
